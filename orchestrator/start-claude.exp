#!/usr/bin/expect -f
set timeout -1
spawn claude --dangerously-skip-permissions {*}$argv
# Wait for the dialog to appear
expect "Yes, I accept"
sleep 0.5
# Send down arrow: ESC (27) + [ + B
send [format %c 27]
send {[B}
sleep 0.3
# Send Enter to confirm
send "\r"
interact
